((room_def
    (room_id) @name)
@item)

((item_def
    (item_id) @name)
@item)

((npc_def
    (npc_id) @name)
@item)

((goal_def
    (goal_block
        (goal_name_stmt (entity_name) @name)))
@item)

((spinner_def
    (spinner) @name)
@item)

((trigger_def
    (entity_name) @name)
@item)
